# Handoffs

Store durable packets exchanged with external model seats here when they do not belong inside one
target repository. Use a mission slug, then identify the seat and purpose in each filename.

Fresh reviews should receive the original request, full review set, diff, acceptance criteria, and
gate output. Do not include the builder's reasoning.
